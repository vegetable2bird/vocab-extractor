# 生词提取器 · Vocab Extractor

面向**中英夹杂的外刊文章**（含中文译文）的生词提取工具。粘贴或导入文章后，点选不认识的英文单词，一键生成生词列表，拿去交给 AI 解析音标 / 词性 / 释义 / 例句。

## 功能

### 1. 多种导入方式

| 方式 | 说明 |
| --- | --- |
| 粘贴文本 | 直接贴外刊文章（中英对照亦可） |
| 上传文件 | `.txt` / `.md` / `.pdf`（PDF 走 PDF.js，浏览器内抽取文字，需联网一次） |
| **截图 / 图片** | **Ctrl+V 直接粘贴截图**，或点按钮选图、拖拽图片；浏览器内 OCR 识别 |
| 拖拽 | 把文件直接拖进输入框 |

### 2. 点选生词 → 生词本

- 文章中每个英文单词都可点击，点一下加入生词本，再点取消
- 生词本自动去重并统计出现频次
- 支持「按字母 / 按词频」排序切换

### 3. 智能预选（免去逐页手点）

- 内置约 2000 个常用英文词表 + 词形还原（自动识别 `runs→run`、`cats→cat`）
- 一键把**非常用词按词频排序**全部预选进生词本，只需删掉已认识的词
- 可设「最少出现 N 次」阈值，只留反复出现的高价值词

### 4. 导出

- 复制纯单词列表
- **复制 AI 提示词**（含要求音标 / 词性 / 释义 / 例句的完整模板）
- 下载 `.txt`

## 快速开始

双击 `index.html` 用浏览器打开即可，无需安装、无需后端。

> 首次使用 PDF 或图片导入时需联网一次（加载 PDF.js / Tesseract.js 语言包），之后可离线使用。

## 变成 App 用（无需发行 / 无需打包）

### 手机：添加到主屏幕

1. 手机浏览器打开 <http://154.8.220.104:8080/>
2. 菜单 →「添加到主屏幕」（iOS Safari：分享 → 添加到主屏幕）
3. 桌面出现图标，点开全屏运行，没有浏览器地址栏

> 页面已声明 `mobile-web-app-capable` / `apple-mobile-web-app-capable`，添加到主屏后以独立应用方式打开。

### 电脑：装成独立窗口应用

**方式一：直接装（不用下载任何东西）**

1. 用 Edge 或 Chrome 打开 <http://154.8.220.104:8080/>
2. 装成应用
   - Edge →「应用」→「将此站点作为应用安装」
   - Chrome →「保存并分享」→「安装页面为应用…」
3. 桌面 / 开始菜单出现「生词提取器」图标，双击即独立窗口打开

**方式二：本地离线版（长期用推荐）**

下载仓库压缩包解压后，双击 `desktop/启动.cmd`：

- 在本机起一个只监听 `127.0.0.1` 的小服务，再以独立窗口打开工具
- `localhost` 属于浏览器认可的**安全上下文**，因此「安装为应用」必然可用（用 HTTP + IP 访问时浏览器会禁用该入口）
- 断网也能干活（仅首次用 PDF / 截图 OCR 需联网下载识别引擎，之后走浏览器缓存）
- 依赖：本机装有 Node.js

详见 `desktop/使用说明.txt`。

## 部署到服务器（独立静态站点）

线上地址：**http://154.8.220.104:8080/**

采用 **独立端口 + 独立进程 + 独立目录**，与服务器上其他项目（如万象 `:80`）完全隔离，互不影响。

| 项 | 值 |
| --- | --- |
| 访问地址 | `http://154.8.220.104:8080/` |
| 站点目录 | `/opt/vocab-extractor/web/` |
| 服务脚本 | `/opt/vocab-extractor/server.js`（零依赖，仅 Node 内置模块） |
| systemd 单元 | `vocab-extractor.service`（开机自启 + 崩溃自动重启） |
| 工作副本 | `/tmp/vx`（仓库克隆，供拉取更新） |

### 一键更新（日常用这个）

```bash
sh /opt/vocab-extractor/update.sh
```

拉取最新代码（**带 5 次重试**，应对 github.com 间歇性不可达）后自动重新部署。即使拉取失败，线上运行的仍是上一个版本，不受影响。

### 首次部署

```bash
git clone --depth 1 https://github.com/vegetable2bird/vocab-extractor.git /tmp/vx
sh /tmp/vx/deploy/deploy.sh
```

### 运维命令

```bash
systemctl status vocab-extractor     # 查看状态
systemctl restart vocab-extractor    # 重启
journalctl -u vocab-extractor -n 50  # 查看日志
```

> 防火墙需放行 TCP 8080（已在腾讯云轻量应用服务器防火墙中配置）。

## 段落结构还原

外刊 PDF 常把一自然段拆成多个视觉行，直接抽取会糊成一坨。本项目用**行距**判定段落边界：

- **PDF 侧**（`rebuild.py`）：按坐标聚行 → 行距中位数作基准 → 超基准 1.3 倍即断段
- **图片 OCR 侧**（`index.html` 内 `reflowByBoxes`）：不依赖 Tesseract 的纯文本，而是取**词框坐标**自行聚行判段

## OCR 修复流水线（可选，用于 PDF 中嵌为图片的生词）

部分外刊 PDF 的重点生词是**嵌入图片**而非文字层，常规提取会漏掉。流水线脚本：

| 脚本 | 作用 |
| --- | --- |
| `extract_words.py` | PyMuPDF 定位词级图片块 → RapidOCR 双尺度投票识别 → wordfreq 自动采纳；增量存 `slots.json`，支持断点续跑 |
| `make_sheets.py` | 把待校对/可疑项拼成校对图版 |
| `validate_corr.py` | 交叉校验人工校对结果（语言冲突 / 相似度过低 / 缺失） |
| `rebuild.py` | 合并文字层与识别结果，按坐标行**还原段落**，输出完整全文 |

数据文件：`slots.json`（词位坐标 + OCR 结果）、`corrections.json`（人工校对）、`flagged.json`、`review_ids.json`。

### 依赖

```bash
pip install pymupdf rapidocr-onnxruntime wordfreq onnxruntime==1.16.3
```

> 注意：`onnxruntime` 需固定 1.16.3，新版会导致 OCR 初始化 `bad allocation`。

## 文件说明

| 文件 | 说明 |
| --- | --- |
| `index.html` | 工具本体（单文件，原生 JS，无依赖） |
| `27kaoyan-full.txt` | 示例产物：124 页考研外刊全文（段落已还原） |
| `27kaoyan-article.txt` | 示例：仅文章正文部分 |

## 已知限制

- OCR 对倾斜、模糊、低分辨率截图识别率会下降，建议正对截取清晰图片
- 专有名词（品牌名、人名）会被当作生词，导出前扫一眼删掉即可
